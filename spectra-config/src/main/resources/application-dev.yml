# ==================== 项目自定义配置 ====================
spectra:
  security:
    verification-code-hmac-key: ${SECURITY_VERIFICATION_CODE_HMAC_KEY:}
    verification-code-max-attempts: ${SECURITY_VERIFICATION_CODE_MAX_ATTEMPTS:5}
    # 默认 HTTP 开发服务使用普通 Host-only Cookie；生产环境必须切换为 Secure 的 __Host- Cookie。
    refresh-cookie-name: ${SPECTRA_SECURITY_REFRESH_COOKIE_NAME:spectra-refresh}
    refresh-cookie-secure: ${SPECTRA_SECURITY_REFRESH_COOKIE_SECURE:false}
    allow-insecure-refresh-cookie: ${SPECTRA_SECURITY_ALLOW_INSECURE_REFRESH_COOKIE:true}
    refresh-cookie-same-site: ${SPECTRA_SECURITY_REFRESH_COOKIE_SAME_SITE:Strict}
    refresh-cookie-same-site-none-allowed: ${SPECTRA_SECURITY_REFRESH_COOKIE_SAME_SITE_NONE_ALLOWED:false}
    refresh-cookie-path: ${SPECTRA_SECURITY_REFRESH_COOKIE_PATH:/}
    refresh-cookie-domain: ${SPECTRA_SECURITY_REFRESH_COOKIE_DOMAIN:}
    csrf-cookie-name: ${SPECTRA_SECURITY_CSRF_COOKIE_NAME:XSRF-TOKEN}
    csrf-header-name: ${SPECTRA_SECURITY_CSRF_HEADER_NAME:X-XSRF-TOKEN}
    mfa-encryption-key: ${SPECTRA_SECURITY_MFA_ENCRYPTION_KEY:}
    mfa-encryption-key-version: ${SPECTRA_SECURITY_MFA_ENCRYPTION_KEY_VERSION:v1}
    mfa-previous-encryption-key: ${SPECTRA_SECURITY_MFA_PREVIOUS_ENCRYPTION_KEY:}
    mfa-previous-encryption-key-version: ${SPECTRA_SECURITY_MFA_PREVIOUS_ENCRYPTION_KEY_VERSION:}
    mfa-challenge-expire: ${SPECTRA_SECURITY_MFA_CHALLENGE_EXPIRE:300}
    mfa-challenge-max-attempts: ${SPECTRA_SECURITY_MFA_CHALLENGE_MAX_ATTEMPTS:5}
    audit-archive-backend: ${SPECTRA_SECURITY_AUDIT_ARCHIVE_BACKEND:PENDING}
  system:
    base-dir: files
    cors:
      required: ${SPECTRA_CORS_REQUIRED:false}
      origin-patterns: ${SPECTRA_CORS_ORIGIN_ALLOWLIST:http://localhost:5173,http://127.0.0.1:5173}
  user:
    default-password: ${DEFAULT_PASSWORD}
  kaptcha:
    type: math
    verify: false
  file:
    upload:
      local:
        upload-dir: ${spectra.system.base-dir}/uploads
        upload-temp-dir: ${spectra.system.base-dir}/temp
      s3:
        endpoint: ${S3_ENDPOINT}
        accessKey: ${S3_ACCESS_KEY}
        secretKey: ${S3_SECRET_KEY}
        bucket: ${S3_BUCKET}
        archive-bucket: ${S3_SECURITY_AUDIT_ARCHIVE_BUCKET:}
        archive-prefix: ${S3_SECURITY_AUDIT_ARCHIVE_PREFIX:security-audit/}
        region: ${S3_REGION}
      default-storage: s3
  ai:
    rag:
      api-key: ${RAG_KEY}
      base-url: ${RAG_BASE_URL}
      model-name: ${RAG_MODEL}
      embedding-batch-size: 20
      max-segment-size: 300
      max-overlap-size: 30
  notification:
    enabled: ${NOTIFICATION_ENABLED:true}
    address-encryption-key: ${NOTIFICATION_ADDRESS_ENCRYPTION_KEY:}
    sensitive-payload-key: ${NOTIFICATION_SENSITIVE_PAYLOAD_KEY:}
    allowed-link-prefixes: ${NOTIFICATION_ALLOWED_LINK_PREFIXES:/login,/auth/,/oa/,/workflow/,/notification/,/notification-center/,/system/,/file/,/ai/}
    worker:
      fixed-delay-ms: ${NOTIFICATION_WORKER_FIXED_DELAY_MS:5000}
      processing-timeout-seconds: ${NOTIFICATION_WORKER_PROCESSING_TIMEOUT_SECONDS:300}
      retry-base-delay-seconds: ${NOTIFICATION_WORKER_RETRY_BASE_DELAY_SECONDS:5}
    cleanup:
      enabled: ${NOTIFICATION_CLEANUP_ENABLED:true}
      fixed-delay-ms: ${NOTIFICATION_CLEANUP_FIXED_DELAY_MS:3600000}
      batch-size: ${NOTIFICATION_CLEANUP_BATCH_SIZE:100}
      retention-seconds: ${NOTIFICATION_CLEANUP_RETENTION_SECONDS:86400}

# ==================== 服务器配置 ====================
server:
  port: ${SERVER_PORT:4004}
  forward-headers-strategy: ${SERVER_FORWARD_HEADERS_STRATEGY:framework}
#  ssl:
#    key-store: ${spectra.system.base-dir}/ssl/keystore.p12
#    key-store-password: ${SSL_PASSWORD}
#    key-store-type: ${SSL_TYPE}
#    key-alias: ${SSL_ALIAS}
  servlet:
    context-path: /api
    session:
      cookie:
        name: JSESSIONID
        path: /
        same-site: none
        secure: true
        http-only: true

# ==================== Spring 配置 ====================
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 10MB
  threads:
    virtual:
      enabled: true
  application:
    name: spectra-admin
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      data-source-properties:
        currentSchema: public
        reWriteBatchedInserts: true
        binaryTransfer: true
        tcpKeepAlive: true
        connectTimeout: 10
        socketTimeout: 60
        ApplicationName: ${spring.application.name}
        loginTimeout: 10
        sslmode: disable
        prepareThreshold: 5
  flyway:
    enabled: ${SPECTRA_FLYWAY_ENABLED:true}
    locations: classpath:db/migration
    baseline-on-migrate: false
    validate-on-migrate: true
    out-of-order: false
    clean-disabled: true
  data:
    redis:
      url: redis://${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}
      timeout: 10s
      connect-timeout: 5s
      lettuce:
        pool:
          enabled: true
          max-idle: 10
          min-idle: 2
          max-active: 20
          max-wait: 5000ms
        shutdown-timeout: 100ms
  security:
    user:
      name:
      password:
  cache:
    type: redis

# ==================== MyBatis-Plus 配置 ====================
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.devops00.**.entity
  type-handlers-package: com.devops00.spectra.common.mybatis.handler
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    local-cache-scope: statement
  global-config:
    banner: false
    db-config:
      id-type: assign_id

# ==================== XSS 防范 ====================
mica:
  xss:
    enabled: true
    path-patterns: /**
    path-exclude-patterns:

# ==================== Flowable 工作流 ====================
flowable:
  database-schema: spectra_workflow
  database-schema-update: true
  jpa-enabled: false
  process-definition-location-prefix: classpath*:/processes/
  async-executor-activate: true
  history-level: full
  eventregistry:
    enabled: false
  idm:
    enabled: false
  app:
    enabled: false
  dmn:
    enabled: false
  cmmn:
    enabled: false

# ==================== LangChain4j / AI ====================
langchain4j:
  open-ai:
    chat-model:
      base-url: ${AI_BASE_URL}
      api-key: ${AI_KEY}
      model-name: ${AI_MODEL}
      log-requests: true
      log-responses: true
    streaming-chat-model:
      base-url: ${AI_BASE_URL}
      api-key: ${AI_KEY}
      model-name: ${AI_MODEL}

# ==================== 日志配置 ====================
logging:
  group:
    spectra: >-
      com.devops00.spectra
    other:
      - com.ulisesbocchio.jasyptspringboot.configuration
      - io.agentscope.harness.agent.middleware
  level:
    root: info
    other: error
    spectra: debug

# ==================== 健康检查 ====================
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
