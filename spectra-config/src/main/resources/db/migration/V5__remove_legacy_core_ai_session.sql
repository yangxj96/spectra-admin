-- AI conversation persistence is owned by spectra_ai.
-- spectra_core.ai_session is a legacy table from the pre-target schema and
-- has no remaining application entity or mapper references.
DROP TABLE spectra_core.ai_session;
