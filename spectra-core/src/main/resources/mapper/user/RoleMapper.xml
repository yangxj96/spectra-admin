<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yangxj96.spectra.core.user.mapper.RoleMapper">
    <!-- @formatter:off -->
    <resultMap id="BaseResultMap" type="com.yangxj96.spectra.core.user.javabean.entity.Role">
        <!--@mbg.generated-->
        <!--@Table "T_SYS_ROLE"-->
        <id column="ID"             jdbcType="BIGINT"    property="id"/>
        <result column="NAME"       jdbcType="VARCHAR"   property="name"/>
        <result column="CODE"       jdbcType="VARCHAR"   property="code"/>
        <result column="STATE"      jdbcType="BIT"       property="state"/>
        <result column="SCOPE"      jdbcType="SMALLINT"  property="scope"/>
        <result column="REMARK"     jdbcType="VARCHAR"   property="remark"/>
        <result column="CREATED_BY" jdbcType="BIGINT"    property="createdBy"/>
        <result column="CREATED_AT" jdbcType="TIMESTAMP" property="createdAt"/>
        <result column="UPDATED_BY" jdbcType="BIGINT"    property="updatedBy"/>
        <result column="UPDATED_AT" jdbcType="TIMESTAMP" property="updatedAt"/>
        <result column="DELETED"    jdbcType="TIMESTAMP" property="deleted"/>
    </resultMap>
    <!-- @formatter:on -->
    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        ID,
        NAME,
        CODE,
        STATE,
        SCOPE,
        REMARK,
        CREATED_BY,
        CREATED_AT,
        UPDATED_BY,
        UPDATED_AT,
        DELETED
    </sql>

    <!-- state在DM中是BIT类型 0=false 非0=true -->
    <select id="getByUserId" resultMap="BaseResultMap">
        SELECT R.ID,
               R.NAME,
               R.CODE,
               R.STATE,
               R.SCOPE,
               R.REMARK,
               R.CREATED_BY,
               R.CREATED_AT,
               R.UPDATED_BY,
               R.UPDATED_AT,
               R.DELETED
        FROM SPECTRA.T_SYS_ROLE R,
             SPECTRA.T_SYS_USER_ROLE_MAP ARM
        WHERE ARM.USER_ID = #{userId}
          AND ARM.ROLE_ID = R."ID"
          AND R.STATE = 1
          AND R.DELETED IS NULL
          AND ARM.DELETED IS NULL
        ORDER BY CREATED_AT DESC;
    </select>

    <update id="removeRelevanceRoles">
        UPDATE SPECTRA.T_SYS_USER_ROLE_MAP
        SET DELETED = CURRENT_TIMESTAMP
        WHERE USER_ID = #{userId}
    </update>

    <insert id="insertRelevanceRole">
        INSERT INTO SPECTRA.T_SYS_USER_ROLE_MAP(ID, USER_ID, ROLE_ID, CREATED_BY, CREATED_AT, UPDATED_BY, UPDATED_AT)
        VALUES (#{id}, #{uid}, #{rid}, 0, CURRENT_TIMESTAMP, 0, CURRENT_TIMESTAMP)
    </insert>
</mapper>