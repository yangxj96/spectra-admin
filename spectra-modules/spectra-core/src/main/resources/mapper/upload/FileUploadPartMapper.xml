<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~  Copyright 2018-2026 yangxj96
  ~
  ~  Licensed under the Apache License, Version 2.0 (the "License");
  ~  you may not use this file except in compliance with the License.
  ~  You may obtain a copy of the License at
  ~
  ~      http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~  Unless required by applicable law or agreed to in writing, software
  ~  distributed under the License is distributed on an "AS IS" BASIS,
  ~  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~  See the License for the specific language governing permissions and
  ~  limitations under the License.
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.devops00.spectra.core.upload.mapper.FileUploadPartMapper">
    <!-- @formatter:off -->
    <resultMap id="BaseResultMap"
               extends="com.devops00.spectra.common.base.BaseMapper.BaseAuditMap"
               type="com.devops00.spectra.core.upload.javabean.entity.FileUploadPart">
        <!--@mbg.generated-->
        <!--@Table spectra_core.file_upload_part-->
        <result column="upload_session_id" property="uploadSessionId" typeHandler="com.devops00.spectra.framework.persistence.mybatis.handler.UUIDTypeHandler"/>
        <result column="part_number"       property="partNumber"/>
        <result column="expected_size"     property="expectedSize"/>
        <result column="expected_sha256"   property="expectedSha256"/>
        <result column="uploaded_size"     property="uploadedSize"/>
        <result column="actual_sha256"     property="actualSha256"/>
        <result column="provider_etag"     property="providerEtag"/>
        <result column="status"            property="status"/>
        <result column="upload_attempt"    property="uploadAttempt"/>
        <result column="uploaded_at"       property="uploadedAt"/>
    </resultMap>
    <!-- @formatter:on -->

    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        upload_session_id,
        part_number,
        expected_size,
        expected_sha256,
        uploaded_size,
        actual_sha256,
        provider_etag,
        status,
        upload_attempt,
        uploaded_at,
        <include refid="com.devops00.spectra.common.base.BaseMapper.BaseAuditColumns"/>
    </sql>

    <select id="findBySessionId" resultType="com.devops00.spectra.core.upload.javabean.entity.FileUploadPart">
        SELECT * FROM spectra_core.file_upload_part
        WHERE upload_session_id = #{sessionId} AND deleted IS NULL
        ORDER BY part_number
    </select>

    <select id="selectForUpdate" resultType="com.devops00.spectra.core.upload.javabean.entity.FileUploadPart">
        SELECT * FROM spectra_core.file_upload_part
        WHERE upload_session_id = #{sessionId} AND part_number = #{partNumber} AND deleted IS NULL
        FOR UPDATE
    </select>

    <select id="countConfirmed" resultType="int">
        SELECT COUNT(*) FROM spectra_core.file_upload_part
        WHERE upload_session_id = #{sessionId} AND status = 'CONFIRMED' AND deleted IS NULL
    </select>

    <update id="prepareTarget">
        UPDATE spectra_core.file_upload_part
        SET expected_sha256 = #{sha256}, upload_attempt = #{attempt}, updated_at = CURRENT_TIMESTAMP
        WHERE upload_session_id = #{sessionId} AND part_number = #{partNumber}
          AND status &lt;&gt; 'CONFIRMED' AND deleted IS NULL
    </update>

    <update id="markUploaded">
        UPDATE spectra_core.file_upload_part
        SET uploaded_size = #{size}, actual_sha256 = #{sha256}, provider_etag = #{etag},
            status = 'UPLOADED', uploaded_at = CURRENT_TIMESTAMP, updated_at = CURRENT_TIMESTAMP
        WHERE upload_session_id = #{sessionId} AND part_number = #{partNumber}
          AND status &lt;&gt; 'CONFIRMED' AND deleted IS NULL
    </update>

    <update id="markConfirmed">
        UPDATE spectra_core.file_upload_part
        SET status = 'CONFIRMED', uploaded_size = #{size}, actual_sha256 = #{sha256}, provider_etag = #{etag},
            uploaded_at = COALESCE(uploaded_at, CURRENT_TIMESTAMP), updated_at = CURRENT_TIMESTAMP
        WHERE upload_session_id = #{sessionId} AND part_number = #{partNumber}
          AND status IN ('UPLOADED', 'CONFIRMED') AND deleted IS NULL
    </update>

    <update id="markExternalConfirmed">
        UPDATE spectra_core.file_upload_part
        SET status = 'CONFIRMED', uploaded_size = #{size}, actual_sha256 = #{sha256}, provider_etag = #{etag},
            uploaded_at = COALESCE(uploaded_at, CURRENT_TIMESTAMP), updated_at = CURRENT_TIMESTAMP
        WHERE upload_session_id = #{sessionId} AND part_number = #{partNumber}
          AND status = 'PENDING' AND deleted IS NULL
    </update>
</mapper>
